class EmbeddedIconModel {
  final iconText;
  final assetLocation;
  final isEmbedded;
  final String base64;

  EmbeddedIconModel({this.iconText, this.assetLocation, this.isEmbedded, this.base64});
}